<li>
	{{burger_name}}

	<button type="button" class="btn btn-light change-devoured" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR DA BURGER!!!{{else}}COOK DA BURGER AGAIN!!!{{/if}}
	</button>
</li>